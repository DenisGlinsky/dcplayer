# SecurityEvent

## Назначение

Безопасный журналируемый event для secure и operational plane.

## Канонические поля

- `event_id` — UUID строка
- `event_type` — строковый enum
- `severity` — строковый enum
- `event_time_utc` — UTC ISO-8601
- `actor_id` — строка
- `redacted_details` — объект

## Инварианты

- Event time обязателен.
- Подробности redacted; секрета в теле события быть не должно.

## Связи с другими объектами

- Экспортируется через AuditExport.
- Может поднимать OperatorAlert.

## Каноническая сериализация

- JSON snake_case.

## Причины отклонения / ошибки

- отсутствует обязательное поле;
- нарушен инвариант;
- формат времени, enum или идентификатора невалиден;
- объект противоречит связанным контрактам;
- сериализация недетерминирована.

## Замечания по эволюции

- Обратимо-совместимые расширения добавляются новыми полями.
- Ломающие изменения требуют явного обновления docs/PLAN.md, docs/STATUS.md и связанных task-specific AGENTS.md.
- При изменении security semantics нужно дополнительно проверить trust boundary и audit model.
